"use strict";

const name = prompt("Insert your name!");
const age = Number(prompt("Insert you age"));
const major = prompt("Insert your major!");

document.write(`name is ${name}, age is ${age}, major is ${major}`);
